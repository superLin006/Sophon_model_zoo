# vits-melo-tts-zh_en — ONNX 处理环境（实测版本，源：conda sophon-vits-melo-tts-zh-en）
# 新建环境：
#   conda create -n sophon-vits-melo-tts-zh-en python=3.10 -y
#   conda run -n sophon-vits-melo-tts-zh-en python -m pip install -r requirements.txt
# 说明：本文件覆盖 ONNX 导出后处理、三段拆分和 ONNX 验证。
# export_onnx.py 还需要与上游 MeloTTS 源码匹配的 PyTorch 依赖，详见 python/README.md；
# 该上游依赖不参与 make_tpu_model.py / make_split_models.py / test_onnx.py。
onnx==1.21.0
onnxruntime==1.23.2
onnxsim==0.6.3
numpy==2.2.6
scipy==1.15.3
soundfile==0.13.1
jieba==0.42.1