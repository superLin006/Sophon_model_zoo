# 模型权重与 bmodel（本地生成，不入库）；保留必需小文件（ids/mel/tokenizer/config）
models/*.safetensors
models/BM1684X/*.bmodel
models/README.md

# 编译中间产物
compile/tmp/

# 运行时 dump
python/dump/

# 交叉编译产物
cpp/build/

# Python
__pycache__/
*.pyc
# W4F16 实验生成物
compile/*.json
compile/*.mlir
compile/*.npz
compile/*.txt
compile/*.prototxt
compile/w4f16_llm_*/
models_llm_std_f16/
test_data/bench*/
test_outputs/
