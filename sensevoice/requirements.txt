# sensevoice — ONNX 导出环境（实测版本，源：conda sophon-sensevoice）
# 新建环境：
#   conda create -n sophon-sensevoice python=3.10 -y
#   conda run -n sophon-sensevoice python -m pip install -r requirements.txt
# 说明：funasr 用于加载 iic/SenseVoiceSmall 权重与预处理；torch 走 CPU 官方源。
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
torchaudio==2.11.0+cpu
transformers==5.14.1
funasr==1.3.1
onnx==1.21.0
onnxruntime==1.23.2
onnxsim==0.6.3
onnxscript==0.7.0
numpy==2.2.6
soundfile==0.13.1
tqdm==4.67.3