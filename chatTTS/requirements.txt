# chatTTS — ONNX 导出（tools/） + Python 推理 + 音色评估（实测版本，源：conda sophon-chattts）
# 新建环境：
#   conda create -n sophon-chattts python=3.10 -y
#   conda run -n sophon-chattts python -m pip install -r requirements.txt
# 说明：覆盖
#   - tools/export_onnx_merged.py 导出（需自备 GPT/Decoder/Vocos checkpoint，见 README）
#   - python/ChatTTS 推理（sail 运行时依赖 libsophon，不在 pip 内）
#   - eval_speaker_sim.py 音色评估（speechbrain 按需下载）
#   torch 走 CPU 官方源；numpy 需 <2.0（vector_quantize_pytorch 兼容性）。
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
torchaudio==2.11.0+cpu
transformers==4.41.1
onnx==1.21.0
onnxruntime==1.23.2
onnxsim==0.6.3
onnxscript==0.7.0
numpy==1.26.4
soundfile==0.13.1
tqdm==4.67.3
pybase16384==0.3.9
vector-quantize-pytorch==1.29.0
vocos==0.1.0
numba==0.65.1
pydub==0.25.1